# Zależności
node_modules/

# Skopiowany APK (wersja do pobrania trafia do GitHub Releases)
Przewodnik-Hiszpania.apk
*.apk

# Klucz do podpisywania — NIE commitować (tajemnica)
android/release.keystore

# Zasoby webowe kopiowane przez `cap sync` (regenerowane przy buildzie)
android/app/src/main/assets/public/
android/app/src/main/assets/capacitor.config.json
android/app/src/main/assets/capacitor.plugins.json
android/app/src/main/res/xml/config.xml